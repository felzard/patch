name: Global (CI/CD)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  patch-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install apkeep
        run: |
          sudo apt-get update
          sudo apt-get install -y cargo
          cargo install apkeep

      - name: Download APK/XAPK from APKPure
        run: |
          mkdir -p downloads
          apkeep -a com.sega.ColorfulStage.en -d apk-pure downloads

      - name: Install apktool
        run: |
          wget https://raw.githubusercontent.com/iBotPeaches/Apktool/master/scripts/linux/apktool -O apktool
          wget https://bitbucket.org/iBotPeaches/apktool/downloads/apktool_2.9.0.jar -O apktool.jar
          chmod +x apktool
          sudo mv apktool apktool.jar /usr/local/bin/

      - name: Install Wine
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y wine wine32 wine64 winbind

      - name: Process and Decompile APK/XAPK
        run: |
          set -e
          FILE=$(ls downloads/*.{apk,xapk} 2>/dev/null | head -n1)
          if [ -z "$FILE" ]; then echo "❌ No .apk or .xapk found"; exit 1; fi
          WORKDIR=$(pwd)

          if [[ "$FILE" == *.xapk ]]; then
            echo "📦 Detected XAPK, extracting..."
            TMPDIR=$(mktemp -d)
            unzip -q "$FILE" -d "$TMPDIR"

            echo "📥 Extracting and processing APKs inside XAPK..."
            APK_LIST=($(find "$TMPDIR" -type f -name "*.apk"))
            if [ ${#APK_LIST[@]} -eq 0 ]; then echo "❌ No APKs found inside XAPK"; exit 1; fi

            echo "🔧 Decompiling base APK (${APK_LIST[0]}) → decompiled/"
            apktool d "${APK_LIST[0]}" -o decompiled

            echo "🔗 Merging split APK contents into base decompiled folder..."
            for ((i=1; i<${#APK_LIST[@]}; i++)); do
              echo "➕ Merging: ${APK_LIST[i]}"
              unzip -o "${APK_LIST[i]}" -d tmp_extract
              ls -R tmp_extract/lib || echo "❌ No libs in this split"
              cp -r tmp_extract/lib/* decompiled/lib/ 2>/dev/null || true
              cp -r tmp_extract/assets/* decompiled/assets/ 2>/dev/null || true
              cp -r tmp_extract/res/* decompiled/res/ 2>/dev/null || true
              rm -rf tmp_extract
            done
          else
            echo "🔧 Decompiling single APK → decompiled/"
            apktool d "$FILE" -o decompiled
          fi

      - name: List all .so files
        run: find decompiled/ -type f -name "*.so"

      - name: Patch libil2cpp.so (if exists)
        run: |
          set -e
          echo "🔍 Searching for libil2cpp.so in decompiled APK..."
          find decompiled/ -type f -name "libil2cpp.so" || echo "No .so found."

          LIB_PATH=$(find decompiled/ -type f -path "*/libil2cpp.so" | head -n1)
          if [ -z "$LIB_PATH" ]; then
            echo "❌ libil2cpp.so not found in decompiled directory"
            exit 1
          fi

          echo "🔧 Found libil2cpp.so at $LIB_PATH, patching..."
          curl -L -o ProjectSekai.exe https://github.com/felzard/patch/releases/download/1/ProjectSekai.exe
          cd "$(dirname "$LIB_PATH")"
          wine "$OLDPWD/ProjectSekai.exe" libil2cpp.so
          rm libil2cpp.so "$OLDPWD/ProjectSekai.exe"
          mv libil2cpp_patched.so libil2cpp.so

      - name: Rebuild unsigned APK
        run: |
          apktool b decompiled -o patched-unsigned.apk

      - name: Generate debug keystore
        run: |
          keytool -genkey -v \
            -keystore debug.keystore \
            -alias androiddebugkey \
            -keyalg RSA -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Android Debug,O=Android,C=US"

      - name: Sign APK with debug keystore
        run: |
          jarsigner \
            -verbose \
            -sigalg SHA1withRSA \
            -digestalg SHA1 \
            -keystore debug.keystore \
            -storepass android \
            -keypass android \
            patched-unsigned.apk \
            androiddebugkey

      - name: Align signed APK
        run: |
          sudo apt-get install -y zipalign
          if ! command -v zipalign &> /dev/null; then
            echo "❌ zipalign not found in PATH"
            exit 1
          fi
          zipalign -v -p 4 patched-unsigned.apk patched-signed.apk

      - name: Upload Unsigned APK
        uses: actions/upload-artifact@v4
        with:
          name: ProjectSekai-Unsigned-APK
          path: patched-unsigned.apk

      - name: Upload Signed APK
        uses: actions/upload-artifact@v4
        with:
          name: ProjectSekai-Signed-APK
          path: patched-signed.apk

      - name: Run Android Emulator and Install APK
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: google_apis
          arch: x86_64
          profile: pixel_3
          force-avd-creation: true
          disable-animations: true
          emulator-options: "-no-snapshot-save -noaudio -no-boot-anim -accel auto"
          script: |
            echo "✅ Emulator ready, installing APK..."
            adb wait-for-device
            adb devices
            adb install -r patched-signed.apk
      
            echo "📱 Verifying install..."
            adb shell pm list packages | grep com.sega.ColorfulStage.en || {
              echo "❌ APK not installed"; exit 1;
            }
      
            echo "🎉 Successfully installed on emulator!"

